{
  "name": "audiofiler-fs",
  "version": "1.0.0",
  "description": "serve staic audio files",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "ts-node app.ts",
    "reboot": "git fetch origin main && git reset origin/main --hard && forever stopall && forever start -v -c ts-node app.ts"
  },
  "author": "ragtag records",
  "license": "ISC",
  "dependencies": {
    "@types/body-parser": "^1.19.2",
    "@types/cors": "^2.8.12",
    "@types/express-fileupload": "^1.2.2",
    "typescript": "^4.7.4",
    "ts-node": "^10.8.2",
    "cors": "^2.8.5",
    "express": "^4.17.3",
    "express-fileupload": "^1.3.1"
  },
  "devDependencies": {
  }
}
